[project]
name = "llm-generate-proxy"
version = "0.1.0"
description = "通用大模型 API 转发代理 - 简化版"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.5",
    "openai>=2.32.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
proxy = "main:main"
